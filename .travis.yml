os: linux
dist: trusty
language: R
sudo: false
latex: false
fortran: false

jobs:
  include:
  - os: osx
  - r: 3.5.2
    after_success: 
    - Rscript -e 'covr::codecov()'
  - r: release
    env:
      - DEVEL_PACKAGES=true
    r_github_packages:
      - RcppCore/Rcpp


