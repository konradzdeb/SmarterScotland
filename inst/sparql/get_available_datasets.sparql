# Simple query getting available data sets
 # Does not provide filter

PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

SELECT DISTINCT ?dataset
WHERE {
				?dataset rdf:type <http://publishmydata.com/def/dataset#Dataset>
}
ORDER BY ?dataset
